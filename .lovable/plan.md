

## Plan: Add MindShare Project at the Top

The previous plan had it appended to the end. Since you want it at the top, I'll adjust:

1. **Add `Heart` import** to the Lucide import line
2. **Insert the MindShare project object as the first item** in the `projects` array (before "Health System Transformation")
3. **Rebalance colors** — with 7 cards, the new top row will be MindShare + AI Scribe. I'll assign MindShare `bg-accent` (peach) so it pairs well with AI Scribe's existing `bg-secondary` (lavender), keeping the alternating pattern intact through the rest of the grid.

Everything else (styling, formatting, icons, grid layout) stays the same. The 7th card will sit alone on the bottom row in the 2-column grid.

